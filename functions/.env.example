# Copy to `functions/.env` (gitignored). Used for web push click-through URLs.
# No trailing slash. Example: https://il-tour-di-paolo.vercel.app
#
# https://firebase.google.com/docs/functions/config-env
WEB_APP_ORIGIN=
